{
  "name": "dx-scaffold",
  "version": "1.0.0",
  "description": "Standalone scaffolding utility for dx-aem-flow plugin ecosystem — creates the same project structure as /dx-init + /aem-init without requiring Claude Code",
  "bin": {
    "dx-scaffold": "./bin/dx-scaffold.js"
  },
  "main": "./lib/index.js",
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "private": true
}
